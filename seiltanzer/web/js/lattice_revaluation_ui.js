// Probability Lattice v25 renders entry / average / current and live mass flow
// directly inside lattice.js. This legacy injected module intentionally stays
// empty so older cached HTML cannot append a second panel below the canvas.
