interface:
  display_name: "Build"
  short_description: "Plan, harden, implement, and review from one spec"
  default_prompt: "Use this Build skill in auto mode to start at the first incomplete phase and deliver the requested outcome."
policy:
  allow_implicit_invocation: false
